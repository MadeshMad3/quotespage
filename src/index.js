import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import QueryQuote from './QueryQuotes'

ReactDOM.render(
  <React.StrictMode>
    <QueryQuote />
  </React.StrictMode>,
  document.getElementById('root')
);
